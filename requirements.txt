# Dependencies are split by role under requirements/ so the runtime image can
# install what serves traffic without the harvest stack's headless Chromium:
#
#   requirements/base.txt      config, database, LLM client
#   requirements/web.txt       base + FastAPI, Jinja, sentence-transformers, CPU torch
#   requirements/harvest.txt   base + Playwright, selectolax, pypdf
#   requirements/dev.txt       everything, plus pytest and ruff
#
# This file keeps the familiar `pip install -r requirements.txt` working and
# resolves to the full development set.

-r requirements/dev.txt
