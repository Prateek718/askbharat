# Core
pydantic>=2.9
httpx>=0.27
PyYAML>=6.0

# Database
SQLAlchemy>=2.0
psycopg[binary]>=3.2
pgvector>=0.3
alembic>=1.13

# LLM (OpenRouter is OpenAI-API-compatible)
openai>=1.50

# Ingest
selectolax>=0.3
crawl4ai>=0.4

# Web — server-rendered, no build step, no client framework
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1

# Dev
pytest>=8.0
ruff>=0.6
pdfplumber>=0.11

# Retrieval — hybrid (lexical + vector) with cross-encoder reranking
sentence-transformers>=3.0
# CPU build; install with:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
torch>=2.0
